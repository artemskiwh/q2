import { CartView } from "@/components/CartView";

export const metadata = {
  title: "Корзина",
  description: "Корзина TYAG Moskva — оформление оптового заказа.",
};

export default function CartPage() {
  return <CartView />;
}
