name: Fetch product images

# Manual trigger only — click "Run workflow" in the Actions tab.
on:
  workflow_dispatch:
    inputs:
      force:
        description: "Re-download images that already exist"
        required: false
        default: "false"
        type: choice
        options: ["false", "true"]

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Download images
        env:
          # Primary source — brand-specific photos from Google image search.
          # Optional — script falls back to Unsplash if these aren't set.
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          GOOGLE_CSE_ID: ${{ secrets.GOOGLE_CSE_ID }}
          # Fallback — generic vape stock photos. Always required so the
          # workflow doesn't fail if Google secrets aren't configured.
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
        run: |
          if [ "${{ inputs.force }}" = "true" ]; then
            node scripts/fetch-product-images.mjs --force
          else
            node scripts/fetch-product-images.mjs
          fi

      - name: Commit and push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/products/*.jpg lib/product-images.ts || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "chore: refresh product images"
          git push origin HEAD:${{ github.ref_name }}
